# opencode-session-memory-sidebar

OpenCode sidebar plugin that shows live session token usage and RAM availability.

## Install via config

```json
{
  "plugin": ["opencode-session-memory-sidebar"]
}
```

After updating config, restart OpenCode.
